{
  "name": "goodreads-hardcover-sync",
  "version": "1.0.0",
  "description": "Sync your Goodreads 'Read' shelf to Hardcover.app automatically.",
  "main": "scripts/sync.js",
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync.js",
    "sync:toread": "node scripts/sync.js --shelf toread",
    "sync:bookclub": "node scripts/sync.js --shelf bookclub"
  },
  "dependencies": {},
  "devDependencies": {
    "dotenv": "^16.4.5"
  }
}
